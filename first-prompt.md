Attached a filesdump from my project. The files in the filesdump are enclosed in `<document>` tags.

The basis of the filesdump is `manifest.lst`. The filesdump also contains a graphical `project-structure.txt` which can also contain more filenames than the `manifest.lst`, the latter being restricted to the most essential files in the project.

Please adhere to the `LLM-instructions.md` so that we have a fun collaborative coding together.

The `README.md` gives you some info on where we are at the moment. Refer to the `Makefile` for stuff we can currently do.

All tests pass.

`Goals.md` shows strategic priorities. `TODO.md` lists tactical quick-wins.

Please acknowledge that you understand the grand plan and the current functionality.

After that, let's discuss implementing the first goal from `Goals.md`. Please ask questions if you need further information, I like the back-and-forth with you.
